// Object

const person = {
    name: 'Tidus',
    age: 20,
    weight: 70.5,
    isAdmin: true
};

console.log(`${person.name} tem ${person.age} anos `);


// Array 

const animals = [ 'Cachorro', 'Gato', 'Passarinho', 'Coelho'];
console.log(animals)
console.log(animals[0]) // Para acessar apenas uma posicão
console.log(animals.length) // Tamanho do array 

