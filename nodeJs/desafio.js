/**
 * Crie 2 arquivos JS.
 * O primeiro, irá exportar uma funcao chamada getFlag().que receberá um argumento do tipo String.
 * Essa funcao deverá buscar dentro do array a flag e retornar o valor da flag.
 * 
 * O segundo irá importar a funcao e passar a flag desejada que é a String e retornar 
 * o valor da flag.
 * 
 * Iremos rodar no terminal o segundo arquivo passando as flgas --name e --greeting
 * para que seja impresso no terminal a saudacao e o nome da pessoa.
 */


